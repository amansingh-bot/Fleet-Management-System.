server:
  port: 8082

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/fleet_vehicle
    username: root
    password: Amansingh@2020

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

  application:
    name: vehicle-service

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka