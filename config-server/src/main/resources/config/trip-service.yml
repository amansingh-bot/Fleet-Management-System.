server:
  port: 8084

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/fleet_trip
    username: root
    password: Amansingh@2020

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka